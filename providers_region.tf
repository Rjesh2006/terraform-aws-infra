provider "aws" {
  region     = "us-east-1"  # Change to your region
  access_key = "YOUR_ACCESS_KEY"  # here we dont have just for demo 
  secret_key = "YOUR_SECRET_KEY"  # Avoid hardcoding credentials just demo 
}

# so we now that iit will be give us the error while planing Error: Retrienving aws account details


# we defined here that we need the aws region for the us-east-2 only 